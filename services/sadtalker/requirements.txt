# SadTalker Server — pinned dependencies
# Cài theo thứ tự:
#   1. pip install -r requirements.txt
#   2. pip install "numpy==1.26.4" --force-reinstall   ← PIN CUỐI CÙNG sau khi cài xong

# ── Server framework ─────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.22.0
httpx>=0.24.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# ── PyTorch (CUDA 12.1) — bỏ dòng này nếu đã cài sẵn ───────────────────────
# torch==2.5.1+cu121
# torchvision==0.20.1+cu121
# --extra-index-url https://download.pytorch.org/whl/cu121

# ── Audio ────────────────────────────────────────────────────────────────────
pydub>=0.25.1
librosa>=0.9.2
resampy>=0.3.1
imageio>=2.19.3
imageio-ffmpeg>=0.4.7

# ── Vision / Face ────────────────────────────────────────────────────────────
opencv-python>=4.7.0
face_alignment>=1.3.5
kornia>=0.6.8
scikit-image>=0.19.0
Pillow>=9.0

# ── Model ────────────────────────────────────────────────────────────────────
safetensors>=0.3.1
yacs>=0.1.8
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8

# ── Utils ────────────────────────────────────────────────────────────────────
scipy>=1.9.0
numba>=0.56.0

# ── QUAN TRỌNG: Sau khi cài xong, chạy lệnh sau để pin numpy ────────────────
# pip install "numpy==1.26.4" --force-reinstall
